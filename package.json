{
  "name": "gohtmx",
  "version": "1.0.0",
  "description": "TODO: Autocomplete for go and templ TODO: Tailwind TODO: Make an app that works",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently --kill-others \"gow run *.go\" \"nodemon --exec templ generate *.templ -e templ --signal SIGTERM\" \"npx tailwindcss -i ./assets/input.css -o ./static/output.css --watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.2",
    "concurrently": "^8.2.2",
    "tailwindcss": "^3.4.1"
  }
}
